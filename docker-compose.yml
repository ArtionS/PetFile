version: '3.9'

services:
  petfile-web:
    restart: on-failure
    ports:
      - 80:80
    build: 
      context: .
      dockerfile: Dockerfile
    environment:
      - DBNAME
      - DBUSER
      - DBPASS
      - DBHOST
